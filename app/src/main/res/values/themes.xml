<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Your existing app theme (unchanged) -->
    <style name="Theme.GameVault" parent="android:Theme.Material.NoActionBar" />

    <!-- Splash screen theme (new) -->
    <style name="Theme.GameVault.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.GameVault</item>
    </style>
</resources>